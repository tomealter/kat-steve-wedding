<div class="section section--gradient section--rsvp">
  <div class="l-constrain">
    {% if post.rsvp_title %}
      <h1 class="section__title">{{ post.rsvp_title }}</h1>
    {% endif %}
    {% if post.rsvp_form %}
      <div class="section__title">{{ post.rsvp_form|shortcodes }}</div>
    {% endif %}
  </div>
</div>